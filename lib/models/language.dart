class Language {
  final int id;
  final String title;
  final String subtitle;
  bool selected = false;

  Language(this.id, this.title, this.subtitle);
  // Language({required this.id, required this.title, required this.subtitle});
}
